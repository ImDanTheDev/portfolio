export type Post = {
    title: string,
    subtitle: string,
    description: string,
    date: string,
    project_showcase: boolean,
    categories: string[],
    published: boolean,
    slug: string,
}
